meta {
  name: Set USDC conversion
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/margin/usdc
  body: json
  auth: inherit
}

body:json {
  {
    "enabled": true
  }
}

settings {
  encodeUrl: true
}

docs {
  Set USDC conversion

  POST /margin/usdc (requires API key authentication)

  Reference: https://docs.cdp.coinbase.com/exchange/rest-api
}
